/// @description Insert description here
// You can write your code in this editor
draw_self(); 



// Find the monster with the lowest defense 
/*
var lowest_defense = real;
var weakest_monster = noone;
for (var i = 0; i < 3; i++) {
    if (instance_exists(GameManager.opponent_card_set[i])) {
        var defense = GameManager.opponent_card_set[i].defense;
        if (defense < lowest_defense) {
            lowest_defense = defense;
            weakest_monster = GameManager.opponent_card_set[i];
        }
    }
}

// Draw the arrow sprite on top of the object with the lowest defense
if (weakest_monster == GameManager.opponent_card_set[card_number]) {
    draw_sprite(sArrow_simple_down, 0, x, y - 25);
}
*/